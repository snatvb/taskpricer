{
	"name": "taskpricer",
	"version": "1.0.3",
	"description": "give task csv [title, task, ratio] ratio(1-9, 0 will 0 price) and u get [title, task, price]",
	"main": "index.js",
	"bin": {
		"taskpricer": "./index.js"
	},
	"dependencies": {
		"csv": "^2.0.0"
	},
	"devDependencies": {},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"push": "git push origin $(git describe $(git rev-list --tags --max-count=1)) && git push --follow-tags"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/snatvb/taskpricer.git"
	},
	"author": "snatvb",
	"license": "ISC"
}
